it is a coomand where we can use two ways.
to merge the two branches to show for the committed files.
in remote area we created a new file and by using this command we get that file from remote area to local area to show the file list in local area.
